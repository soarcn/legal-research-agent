## Summary

- correct the Weaviate Compose healthcheck to use its versioned readiness endpoint
- fix transaction cleanup in the opt-in PostgreSQL adapter test
- preserve database credentials when constructing isolated Alembic test URLs

## Verification

- `RUN_REAL_INTEGRATION=1 uv run pytest tests/integration -m real_service -q` — 3 passed
- `make check` — 23 passed, 3 skipped
- `make audit`
- `uv lock --check`
- `docker compose config --quiet`

The correction was confirmed against the local Weaviate 1.28.4 service: the unversioned readiness path returned 404 while `/v1/.well-known/ready` returned 200. Named Docker volumes were preserved.
